# 策略与回测日期职责拆分

## 结论

采用用户确认的新方案：策略筛选只拥有训练区间，候选批次回测单独拥有回测区间。

## 对比

V4.4 在筛选时同时填写训练和测试区间，能够一次冻结所有日期，但把尚未创建的回测配置提前放进策略模块，页面含义和模块职责不清。新方案仍保留训练数据隔离和四档价格冻结，同时把回测日期延迟到真正创建回测时填写，职责更直接，也不会让策略接触未来数据。

## 兼容

历史筛选区间中的测试日期继续保留在数据库中供审计，但新筛选不再写入，新回测不再读取。所有新回测必须显式提交与训练区间一一对应的回测日期。
