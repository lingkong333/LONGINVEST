# 需求基线

当前唯一生效的需求与技术基线：

- `A股个股长波段价格提醒系统_需求与技术设计_v3.1_项目实施基线.md`

用户提供的 V3.0 原文保留为不可改的来源版本。V2.1 和旧设计摘要均为历史资料，不能作为独立需求来源。

实现与当前基线不一致时，必须先记录对比结论：

1. 文档方案更好：按文档修改实现和测试。
2. 新方案更好：先发布新的规格版本并把本文件指向新版本，再修改实现和测试。
3. 不允许代码先行后长期补文档，也不允许在模块内部静默偏离公共契约。
